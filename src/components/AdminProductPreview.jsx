export default function AdminProductPreview({ product }) {
  const images = Array.isArray(product.images) ? product.images : []
  const tags = Array.isArray(product.tags) ? product.tags : []
  return <div className="sticky top-8 border border-neutral-800 bg-neutral-950/70 p-6"><p className="text-[10px] uppercase tracking-widest text-amber-400">Live Preview</p>{images[0] ? <img src={images[0]} alt="" className="mt-5 aspect-square w-full object-cover" /> : <div className="mt-5 flex aspect-square items-center justify-center bg-neutral-900 text-xs uppercase tracking-widest text-neutral-600">Add an image</div>}<h2 className="mt-5 font-serif text-2xl">{product.name || 'Untitled Creation'}</h2><p className="mt-2 text-sm text-amber-400">{Number(product.price_dh || 0).toLocaleString()} DH</p><p className="mt-2 text-xs uppercase tracking-widest text-neutral-500">{Number(product.stock || 0) > 0 ? 'In Stock' : 'Out of Stock'}</p><div className="mt-4 flex flex-wrap gap-2">{tags.map((tag) => <span key={tag} className="rounded-full border border-amber-500/50 px-2 py-1 text-[10px] text-amber-300">{tag}</span>)}</div><div className="mt-4 space-y-2 border-t border-neutral-800 pt-4">{Object.entries(product.specifications || {}).map(([key, value]) => <p key={key} className="flex justify-between gap-3 text-xs text-neutral-400"><span>{key}</span><span className="text-neutral-200">{value}</span></p>)}</div></div>
}
